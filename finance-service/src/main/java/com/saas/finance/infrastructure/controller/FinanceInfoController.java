package com.saas.finance.infrastructure.controller;

import com.saas.common.controller.BaseInfoController;
import org.springframework.web.bind.annotation.RestController;

/** Expone /api/info y /api/version del finance-service (heredado de saas-common). */
@RestController
public class FinanceInfoController extends BaseInfoController {
}
