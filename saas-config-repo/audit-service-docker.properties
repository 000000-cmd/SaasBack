# =====================================================================
# AUDIT-SERVICE · PERFIL DOCKER
# BD dedicada saas_audit (host del contenedor MySQL via SAAS_DB_HOST).
# Service + profile => gana sobre application-docker.properties (saas_db).
# =====================================================================
spring.datasource.url=jdbc:mysql://${SAAS_DB_HOST:mysql}:3306/saas_audit?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Bogota&createDatabaseIfNotExist=true
